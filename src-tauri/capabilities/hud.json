{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hud",
  "description": "Capability for the HUD/stats window (client role: reads tama:state, listens agent-status-changed)",
  "windows": ["stats"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:default",
    "store:default"
  ]
}
