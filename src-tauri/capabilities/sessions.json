{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sessions",
  "description": "Capability for the tray sessions popover (listens sessions-snapshot + label-theme-changed, self show/hide; positioning is Rust-side)",
  "windows": ["sessions"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
