{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shop",
  "description": "Capability for the shop window (client role: emits tama:mutate, listens tama:state)",
  "windows": ["shop"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:default"
  ]
}
