# Rust build output (workspace root target — avoids committing huge build dir)
/target

# Sidecar binaries — built locally via scripts/build-sidecars.sh, not committed.
# CI rebuilds them per-platform in build-release.yml before tauri build.
/src-tauri/binaries/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
